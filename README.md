# WhatsApp Worker

Production multi-session WhatsApp worker (Baileys). Replaces the Chrome extensions.

See **[DEPLOY_AWS.md](./DEPLOY_AWS.md)** for full AWS EC2 steps.

```bash
cp .env.example .env
npm install
npm start
```
